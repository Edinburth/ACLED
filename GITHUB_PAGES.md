# GitHub Pages deployment

Generated by Prompt 2 on 2026-04-30.

## Where the page lives

- Repository: https://github.com/Edinburth/ACLED
- Live webpage: https://edinburth.github.io/ACLED/
- Pages source: `main` branch, `/docs` folder.
- Build status at end of Prompt 2: built

## What Prompt 2 changed in this workspace

- Renamed `Docs/` folder to `docs/` (lowercase) so GitHub Pages can serve from it.
- Created `.gitignore` with default rules for macOS, Python, Node, secrets, large data.
- Initialized git repository on `main` branch (already existed with no commits).
- Set git user config (`Edinburth`, `Edinburth@users.noreply.github.com`).
- Made initial commit and created public GitHub repo `Edinburth/ACLED`.
- Enabled GitHub Pages from `main` branch, `/docs` folder via GitHub API.
- Created `GITHUB_PAGES.md` (this file).

## What Prompt 2 did NOT change

- Your `README.md` and every existing file in the project were not modified.
- Your `.py`, `.ipynb`, `.txt`, `.csv`, and `data/` files are untouched.
- Files were renamed (folder only, not moved); your `index.html` still exists in `docs/`.

## How to update the page later

Edit any file, then ask Copilot: *"Commit and push."* The live page rebuilds on its own.

## How to undo

- Disable Pages in the repo settings, or delete the `/docs/` folder and push.
- Delete the repository: `gh repo delete Edinburth/ACLED`.

## Questions

Ask Copilot (me) about any step above. I can explain why each command was run, or help adjust the setup for your specific project.